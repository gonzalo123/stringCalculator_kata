{
    "name": "gonzalo123/kataphp",
    "minimum-stability": "dev",
    "description": "Boostrap for coding katas witn PHP",
    "license": "MIT",
    "keywords": ["cli", "kata", "katayuno"],
    "require": {
        "php": ">=7.0",
        "phpunit/phpunit": "8.5.x-dev"
    },
    "require-dev": {
    },
    "autoload": {
        "psr-0": {
            "": "src/"
        }
    },
    "scripts": {
        "test": "phpunit --colors=always"
    },
    "config": {
        "bin-dir": "bin/"
    }
}
